#include "encoder.h"
#include <button.h>

void setupEncoder(void)
{
  DDRF &= (1 << PF6);
  PORTF |= ( 1 << PF6);

  // DDRF |= (1 <<PF7);
}

void checkEncoderOutputs(void)
{
  //if (BIT_CHECK(PINF,PF6)) {
  //  BIT_SET(PORTF,PF7);
  //}
  //else {
  //  BIT_CLEAR(PORTF,PF7);
  //}
}
